<% if @feed_items.any? %>
  <ol class="microposts" style="color: #FFF; font-family: 'Ubuntu', sans-serif; font-size: 20px;">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items,
                    params: { controller: :users, action: :show } %>

<% end %>
